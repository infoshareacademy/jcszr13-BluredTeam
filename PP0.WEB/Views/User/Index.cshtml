@using PP0.EntityFrameworkCore.Database.Entities;
@model IEnumerable<PP0.EntityFrameworkCore.Database.Entities.User>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.Login)*@
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.Password) *@
            </th>
            <th>
                Roles
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            @* <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                    @foreach (var role in item.Roles)
                    {
                        <p>@role.Name</p>
                    }
            </td>
                <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Login }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Login }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Login })
            </td>
            *@
            </tr>
}
    </tbody>
</table>
